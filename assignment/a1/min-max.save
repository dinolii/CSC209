if 2 * a.index > arr.size and 2 * a.index + 1 > arr.size:
    return a.value
elif 2 * a.index < arr.size and 2 * 2.index + 1 > arr.size:
    return 2 * a.index.value
elif 2 * a.index < arr.size and 2 * 2.index + 1 < arr.size:
   left = min-max(2 * a.index)
   right = min-max(2 * a.index + 1)
   return max(left, right) 
